---
category: introduction
slug: git-le-versioning
type: cours
fileName: 2-cours.mdx
title: Git le versioning
description: Comprendre le versioning avec Git
logoImgSrc: /logo/git_image.png
duration: 1 heure
published: true
---


## Débuter
Pour utiliser Git, il faut d'abord l'installer.

Pour cela, rendez-vous sur [le site officiel de Git](https://git-scm.com/downloads) et téléchargez la version correspondant à votre système d'exploitation.

Après avoir installé Git, vous pouvez commencer à utiliser les commandes de base depuis votre terminal (il faut ajouter le chemin de Git dans les variables d'environnement pour que les commandes soient reconnues).

Il existe des outils intégrés dans les IDE (Integrated Development Environment) ou Github Desktop qui permettent de gérer les dépôts Git de manière plus visuelle, cependant il est important de connaître les commandes de bases.



## Fonctionnement
La première chose à faire est de créer un dépôt.

Pour cela, il suffit de se rendre à partir du terminal dans le dossier de votre projet et de taper la commande suivante :

```bash
git init
```

Après cela 2 commandes sont très utiles :

- Ajouter un fichier en particulier
```bash
git add nom_du_fichier
```


- Ajouter tous les fichiers
```bash
git add .
```


Pour finir, valider les modifications avec la commande :
```bash
git commit -m “Message”
```
Le message doit être le plus descriptif possible.



## Poster son dépôt sur une plateforme
Github est une des plateformes de gestion de dépôts les plus utilisées.

Elle facilite le travail d'équipe grâce à de nombreuses fonctionnalités et son interface user friendly.

Après avoir créé un compte, vous pouvez naviguer section repositories et chercher le bouton Create new repository.

Github vous redirige et vous explique la marche à suivre.

Il faut créer ou lier un dépôt.

Pour lier le dépôt local au dépôt distant, il faut faire la commande :
```bash
git remote add origin lien_du_depôt_Github
```
Cela va enregistrer sous le label origin le dépôt distant. Le label est très important, car vous pouvez lier plusieurs dépôts distants.


Il ne reste plus qu'à s'assurer d'être sur la bonne branche (nous parlerons des branches un peu plus tard) en effectuant la commande :
```bash
git branch -M main
```


Nous sommes prêt, nous pouvons push sur Github notre dépôt local avec la commande :
```bash
git push origin main
```
Cela va merger la branche main locale sur le dépôt distant.


En équipe, avant de push, il faut toujours s'assurer d'être à jour avec le dépôt distant avec la commande :
```bash
git pull origin main
```


## Cloner un dépôt
Il est possible de cloner un dépôt distant sur votre machine avec la commande :
```bash
git clone lienDuDepôtGithub
```
Cela va créer un dossier avec le nom du dépôt et récupérer tout le code.


Ne pas oublier d'exécuter la commande :
```bash
npm install
```
Dans la plupart des projets, il faudra aussi renseigner les variables d'environnement dans le fichier .env à la racine du projet avant de lancer le serveur (pour les projets Node.js en général npm run dev).



## Authentification
Afin de push sur un dépôt distant, il est nécessaire de s'authentifier.

En général, vous êtes redirigé vers le site Github pour vous connecter.

Cependant, si vous avez plusieurs comptes voici quelques commandes qui peuvent vous être utile :
```bash
git config --global user.name “PseudoGithub”
git config --global user.email “EmailGithub”
git config --global credential.username “new_username”
```
Enlevez --global si vous ne voulez changer seulement la configuration du projet dans lequel vous faites la commande.


Il est possible de vérifier les informations avec la commande :
```bash
git config --list
```



## Concepts avancés
Git est un outil très puissant et il est possible de faire beaucoup de choses avec.

Voici quelques concepts avancés :

- Les branches
- Les conflits
- Les tags
- Les pull requests
- Les branches permettent de travailler sur des fonctionnalités sans impacter le code principal.

Cela permet de travailler en parallèle sur plusieurs fonctionnalités.

Les conflits surviennent lorsqu'il y a des modifications sur la même ligne de code.

Git ne sait pas quelle modification garder, il faut donc résoudre le conflit manuellement.

Les tags permettent de marquer un commit. Cela peut être utile pour marquer une version stable par exemple.

Les Pulls Request sont des demandes de fusion de code. Cela permet de vérifier le code avant de le fusionner avec la branche principale.